class Animal {
  constructor({ species, lifeExpectancy, continent, size, isMammal }) {
    this.species = species;
    this.lifeExpectancy = lifeExpectancy;
    this.continent = continent;
    this.size = size;
    this.isMammal = isMammal;
  }
}

module.exports = Animal;
